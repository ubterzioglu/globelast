# 19 Mayıs Premium Globe — Pin Types, Emoji, Popup & Abuse Prevention

## Proje Yapısı Notu

Proje `src/` dizini kullanmıyor. Kök dizinde `app/`, `components/`, `lib/`, `types/` yapısı var.
`newfeature.md`'deki `src/` ön ekli yollar aşağıdaki gibi eşleştirilecek:

| newfeature.md | Gerçek Yol |
|---|---|
| `src/config/pinTypes.ts` | `config/pinTypes.ts` |
| `src/types/eventPin.ts` | `types/pins.ts` (güncelle) |
| `src/components/globe/PinDetailModal.tsx` | `components/globe/PinDetailModal.tsx` |
| `src/lib/validation/pinValidation.ts` | `lib/validation/pinValidation.ts` |

---

## Faz 1 — Config & Types

### TODO 1.1 — `config/pinTypes.ts` oluştur
- [ ] `config/` dizinini oluştur
- [ ] `PIN_TYPES` objesini tanımla (greeting, student, event, family, general)
- [ ] Her pin type için: emoji, label, shortLabel, description, color, glow
- [ ] `PinType` type export et
- [ ] `DEFAULT_PIN_TYPE` export et (`"greeting"`)
- [ ] `PIN_TYPE_OPTIONS` array export et

### TODO 1.2 — `types/pins.ts` güncelle
- [ ] `PinType` import et `config/pinTypes.ts`'den
- [ ] `EventPin` type'a `pin_type: PinType` alanı ekle
- [ ] `EventPin` type'a `moderated_at`, `moderated_by`, `moderation_note`, `last_submitted_at` alanları ekle
- [ ] `PinStatus`'a `"hidden"` ekle (`"pending" | "approved" | "rejected" | "hidden"`)
- [ ] `PublicEventPin` type oluştur (sadece public alanlar: id, display_name, city, country, note, lat, lng, pin_type, event_key, created_at)

---

## Faz 2 — Database Schema Güncellemeleri

### TODO 2.1 — `supabase/schema.sql`'e pin_type alanı ekle
- [ ] `event_pins` tablosuna `pin_type text not null default 'greeting'` ekle
- [ ] `event_pins_pin_type_check` constraint ekle: `pin_type in ('greeting','student','event','family','general')`

### TODO 2.2 — Status constraint güncelle
- [ ] `PinStatus`'a `"hidden"` ekle
- [ ] `event_pins_status_check` constraint güncelle: `status in ('pending','approved','rejected','hidden')`

### TODO 2.3 — One pin per user per event unique index
- [ ] `event_pins_one_pin_per_user_per_event` unique index oluştur: `(user_id, event_key)`

### TODO 2.4 — Moderation fields ekle
- [ ] `moderated_at timestamptz` ekle
- [ ] `moderated_by uuid references auth.users(id)` ekle
- [ ] `moderation_note text` ekle
- [ ] `last_submitted_at timestamptz default now()` ekle

### TODO 2.5 — `pin_reports` tablosu oluştur
- [ ] `id uuid primary key`
- [ ] `pin_id uuid references event_pins(id) on delete cascade`
- [ ] `reporter_user_id uuid references auth.users(id)`
- [ ] `reason text not null`
- [ ] `message text`
- [ ] `created_at timestamptz default now()`
- [ ] `pin_reports_reason_check` constraint: `reason in ('spam','offensive','wrong_location','personal_data','other')`
- [ ] `pin_reports_one_report_per_user_per_pin` unique index: `(pin_id, reporter_user_id) where reporter_user_id is not null`

### TODO 2.6 — RLS policy'leri güncelle
- [ ] Public SELECT policy'yi kontrol et: sadece `status = 'approved'` gösterilmeli
- [ ] User INSERT policy: `auth.uid() = user_id` ve `status = 'pending'` (user kendi pinini approved olarak insert edememeli)
- [ ] User UPDATE policy: kullanıcı `status` alanını değiştirememeli, sadece kendi pinini güncelleyebilmeli

---

## Faz 3 — Server-Side Validation

### TODO 3.1 — `lib/validation/pinValidation.ts` oluştur
- [ ] `lib/validation/` dizinini oluştur
- [ ] `PinPayload` type tanımla
- [ ] `validatePinPayload()` fonksiyonu yaz:
  - pin_type geçerli mi (`PIN_TYPES` içinde mi)
  - city dolu mu (min 2 char)
  - country dolu mu (min 2 char)
  - note uzunluğu 3-180 karakter mi
  - note link içeriyor mu (`BLOCKED_LINK_PATTERNS` kontrolü)
  - note HTML/script içeriyor mu (`/<[^>]*>/g` regex)
  - lat -90 ile 90 arasında mı
  - lng -180 ile 180 arasında mı
- [ ] `containsBlockedLink()` helper
- [ ] `containsHtmlLikeContent()` helper
- [ ] `roundCoordinate()` helper — `toFixed(2)` ile şehir seviyesi normalizasyon
- [ ] `sanitize` objesi döndür (trimmed, validated veri)

---

## Faz 4 — API Routes

### TODO 4.1 — `app/api/pins/route.ts` oluştur (Pin Create)
- [ ] POST handler: auth user kontrolü (Supabase browser client ile)
- [ ] Request body'den payload al
- [ ] `validatePinPayload()` ile validasyon yap
- [ ] User bu event için daha önce pin oluşturmuş mu kontrol et (one pin rule)
- [ ] Cooldown kontrolü: `last_submitted_at` + 5 dakika dolmuş mu
- [ ] Supabase'e insert yap (`status: 'pending'`, `pin_type`, normalized coordinates)
- [ ] Hata mesajlarını Türkçe döndür

### TODO 4.2 — `app/api/pins/[id]/route.ts` oluştur (Pin Update)
- [ ] PATCH handler: auth user kontrolü
- [ ] Pin sahibi mi kontrol et (`user_id === auth.uid()`)
- [ ] `validatePinPayload()` ile validasyon yap
- [ ] Cooldown kontrolü
- [ ] Sadece `city`, `country`, `note`, `pin_type`, `lat`, `lng` güncellenebilsin
- [ ] Güncelleme sonrası `status` tekrar `'pending'` olsun
- [ ] `last_submitted_at` güncelle

### TODO 4.3 — `app/api/pins/my/route.ts` oluştur (Kullanıcının kendi pinini getir)
- [ ] GET handler: auth user kontrolü
- [ ] User'ın bu event için pinini getir (varsa)
- [ ] Pin status, içerik ve güncelleme bilgisi döndür

---

## Faz 5 — Pin Form Güncellemesi

### TODO 5.1 — `PinFormModal.tsx`'e pin type selector ekle
- [ ] `PIN_TYPES` ve `PIN_TYPE_OPTIONS` import et
- [ ] Form state'e `pin_type` ekle, default `'greeting'`
- [ ] Formda 5 seçenekli pin type kartları göster (emoji + shortLabel)
- [ ] Seçili kartta border, glow ve active state
- [ ] Mobilde rahat tıklanabilir boyut

### TODO 5.2 — Pin submit akışını API route'a yönlendir
- [ ] Mevcut doğrudan Supabase insert'i kaldır
- [ ] Yeni pin: POST `/api/pins` çağır
- [ ] Mevcut pin güncelleme: PATCH `/api/pins/[id]` çağır
- [ ] Hata mesajlarını göster (one-pin-rule, cooldown, validation hataları)
- [ ] Başarı sonrası globe refresh

### TODO 5.3 — Note validation UI güncelle
- [ ] Max karakter sayısını 240'ten 180'e düşür
- [ ] Min 3 karakter kontrolü
- [ ] Link engeli uyarısı göster
- [ ] HTML/script engeli uyarısı göster

### TODO 5.4 — Mevcut pin varsa update flow göster
- [ ] `PinLauncher.tsx`'te user'ın mevcut pinini kontrol et (GET `/api/pins/my`)
- [ ] Pin varsa: "Pinini Güncelle" butonu göster, mevcut verilerle formu doldur
- [ ] Pin yoksa: normal "Kendini Pinle" akışı

---

## Faz 6 — Globe Visual Güncellemesi

### TODO 6.1 — `PremiumGlobe.tsx`'te pin renklerini pin_type'a göre ayarla
- [ ] `PIN_TYPES` import et
- [ ] `pointColor`'ı `pin_type` üzerinden hesapla: `PIN_TYPES[pin.pin_type]?.color ?? PIN_TYPES.general.color`
- [ ] Fallback renk: `general` pin type rengi

### TODO 6.2 — Hover/selected pin görsel efektleri
- [ ] `selectedPin` state ekle
- [ ] Selected pin için `pointRadius` ve `pointAltitude` biraz büyüsün
- [ ] Normal pin: `pointRadius=0.28`, `pointAltitude=0.015`
- [ ] Selected pin: `pointRadius=0.42`, `pointAltitude=0.035`

### TODO 6.3 — Tooltip'i pin_type aware yap
- [ ] `createLabel()` fonksiyonunu güncelle
- [ ] Tooltip'e emoji badge ekle (pin type emoji)
- [ ] `escapeHtml` kullanımını koru
- [ ] Note 90 karaktere kısaltılsın

---

## Faz 7 — Premium Pin Detail Modal

### TODO 7.1 — `components/globe/PinDetailModal.tsx` oluştur
- [ ] Modal overlay + glassmorphism card
- [ ] Büyük emoji badge (pin type emoji, 72x72px, glow efekti)
- [ ] Display name (h2)
- [ ] City, country (location text)
- [ ] User note (tırnak içinde, elegant typography)
- [ ] Pin type badge (emoji + label, pill şeklinde)
- [ ] Event name ("19 Mayıs Atatürk'ü Anma, Gençlik ve Spor Bayramı")
- [ ] Close button (sağ üst, yuvarlak, × ikon)

### TODO 7.2 — Modal CSS/Tailwind styling
- [ ] Glassmorphism: `bg-[rgba(15,23,42,0.82)] backdrop-blur-[22px]`
- [ ] Border: `border border-white/14`
- [ ] Rounded: `rounded-[28px]`
- [ ] Shadow: `shadow-[0_30px_100px_rgba(0,0,0,0.55)]`
- [ ] Max-width: 420px
- [ ] Emoji glow: pin type rengine göre `--pin-glow` CSS variable

### TODO 7.3 — Modal interaction rules
- [ ] Pin click → `setSelectedPin(pin)` → modal açılsın
- [ ] ESC tuşu ile kapansın
- [ ] Close button ile kapansın
- [ ] Overlay'e tıklama ile kapansın
- [ ] Modal içeriğe tıklama propagation'ı durdursun

### TODO 7.4 — Mobile bottom sheet styling
- [ ] `@media (max-width: 768px)`: modal fixed, bottom: 12px, left/right: 12px
- [ ] Bottom sheet animasyonu (slide up)

### TODO 7.5 — PremiumGlobe'da mevcut detail panel yerine PinDetailModal kullan
- [ ] Mevcut bottom-right detail panel'i kaldır
- [ ] `selectedPin` state'ini koru
- [ ] `<PinDetailModal>` render et
- [ ] Email, user_id, exact address hiçbir yerde gösterilmesin

---

## Faz 8 — Filter Chips & Stats

### TODO 8.1 — Filter chips component oluştur
- [ ] Globe altına veya sağ panele filtre chip'leri ekle
- [ ] Seçenekler: Tümü, 🇹🇷 Selam, 🎓 Öğrenci, 📍 Etkinlik, 🏠 Aile, 🌍 Genel
- [ ] Aktif filtre belirgin olsun (border + glow)
- [ ] Mobilde yatay kaydırılabilir olsun

### TODO 8.2 — Frontend filtering uygula
- [ ] `selectedPinType` state ekle (default: `"all"`)
- [ ] `visiblePins` hesapla: `selectedPinType === "all" ? pins : pins.filter(...)`
- [ ] Globe'a `visiblePins` verisini gönder

### TODO 8.3 — Stats kartları ekle
- [ ] Globe UI'ında küçük istatistik kartları
- [ ] Toplam pin sayısı + her pin type sayısı
- [ ] Sadece `approved` pinler sayılsın
- [ ] Realtime şart değil, sayfa refresh ile güncelle

---

## Faz 9 — Abuse Prevention Entegrasyonu

### TODO 9.1 — Login gate'i kontrol et
- [ ] `PinLauncher.tsx`'te login kontrolü zaten var, doğrula
- [ ] Pin create/update API route'larında auth kontrolü şart

### TODO 9.2 — One pin per user per event uygula
- [ ] API route'ta mevcut pin kontrolü (TODO 4.1'de)
- [ ] Frontend'de mevcut pin kontrolü (TODO 5.4'te)
- [ ] Unique index ile DB seviyesinde garanti (TODO 2.3'te)

### TODO 9.3 — Cooldown uygula
- [ ] Server-side 5 dakika cooldown (TODO 4.1 ve 4.2'de)
- [ ] Frontend'de cooldown mesajı göster

### TODO 9.4 — Note validation
- [ ] Server-side: 3-180 karakter, linksiz, HTMLsiz (TODO 3.1'de)
- [ ] Frontend'de karakter sayacı ve engeller (TODO 5.3'te)

### TODO 9.5 — Pending approval default
- [ ] Tüm yeni pinler `status: 'pending'` ile insert (TODO 4.1'de)
- [ ] Update sonrası `status` tekrar `'pending'` olsun (TODO 4.2'de)
- [ ] Public globe sadece `approved` pinleri göstersin (zaten mevcut RLS policy)

---

## Faz 10 — Data Security Review

### TODO 10.1 — Public popup güvenlik kontrolü
- [ ] `PinDetailModal`'da email, user_id, exact address yok mu doğrula
- [ ] `dangerouslySetInnerHTML` kullanılmadığını doğrula
- [ ] Tüm kullanıcı verisi React interpolation ile render ediliyor mu kontrol et

### TODO 10.2 — Coordinate normalization
- [ ] API route'ta `roundCoordinate()` ile `.toFixed(2)` uygulandığını doğrula
- [ ] Exact address istenmediğini doğrula (form'da sadece city/country var)

### TODO 10.3 — Service role key güvenliği
- [ ] Service role key'in frontend'de kullanılmadığını doğrula
- [ ] Admin API route'larında auth/admin kontrolü ekle (şu an yok!)

---

## Faz 11 — Report Sistemi (Opsiyonel / Zaman Varsa)

### TODO 11.1 — Report butonu PinDetailModal'a ekle
- [ ] "Report" butonu ekle (küçük, subtle)
- [ ] Report reason seçimi: Spam, Offensive, Wrong location, Personal data, Other

### TODO 11.2 — `app/api/pins/[id]/report/route.ts` oluştur
- [ ] POST handler: auth kontrolü
- [ ] One report per user per pin kuralı
- [ ] `pin_reports` tablosuna insert
- [ ] 3 report sonrası pin otomatik `hidden` yap

---

## Uygulama Sırası

```
Faz 1 (Config & Types) → Faz 2 (Database) → Faz 3 (Validation) → Faz 4 (API Routes)
  → Faz 5 (Pin Form) → Faz 6 (Globe Visual) → Faz 7 (Premium Modal)
  → Faz 8 (Filters & Stats) → Faz 9 (Abuse Prevention) → Faz 10 (Security Review)
  → Faz 11 (Report - opsiyonel)
```

## Mevcut Dosyalar Değişecek

| Dosya | Değişiklik |
|---|---|
| `config/pinTypes.ts` | **YENİ** |
| `lib/validation/pinValidation.ts` | **YENİ** |
| `components/globe/PinDetailModal.tsx` | **YENİ** |
| `app/api/pins/route.ts` | **YENİ** |
| `app/api/pins/[id]/route.ts` | **YENİ** |
| `app/api/pins/my/route.ts` | **YENİ** |
| `types/pins.ts` | Güncelle (pin_type, yeni alanlar, hidden status) |
| `supabase/schema.sql` | Güncelle (pin_type, constraints, reports, moderation) |
| `components/globe/PremiumGlobe.tsx` | Güncelle (renkli pinler, modal, filtreler, stats) |
| `components/pins/PinFormModal.tsx` | Güncelle (pin type selector, API route'a yönlendirme) |
| `components/pins/PinLauncher.tsx` | Güncelle (mevcut pin kontrolü, update flow) |
| `app/api/admin/pins/[id]/approve/route.ts` | Güncelle (admin auth kontrolü ekle) |
| `app/api/admin/pins/[id]/reject/route.ts` | Güncelle (admin auth kontrolü ekle) |
